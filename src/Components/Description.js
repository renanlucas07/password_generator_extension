import React from "react";
import styles from "./Description.modules.css";

const Description = () => {
  return (
    <p className={styles.description}>
      generate your passwords for websites using a secret key and the url of the
      actual site.
    </p>
  );
};

export default Description;
